import 'package:flutter/material.dart';

import '../../config/theme.dart';

Size mediaSize(context) => MediaQuery.sizeOf(context);

TextTheme getThemeData() => themeData.textTheme;

DateTime now = DateTime.now();
